{
  "name": "amazon-inspired-marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "Production-ready MVP marketplace built as a pnpm monorepo with Next.js, Express, MongoDB, Mongoose, TypeScript, Tailwind CSS, and JWT authentication.",
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:web": "pnpm --filter @marketplace/web dev",
    "dev:api": "pnpm --filter @marketplace/api dev",
    "build": "pnpm -r build",
    "start:web": "pnpm --filter @marketplace/web start",
    "start:api": "pnpm --filter @marketplace/api start",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules apps/*/.next apps/*/dist packages/*/dist"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "latest",
    "@typescript-eslint/parser": "latest",
    "eslint": "latest",
    "typescript": "latest",
    "@eslint/js": "latest"
  }
}
