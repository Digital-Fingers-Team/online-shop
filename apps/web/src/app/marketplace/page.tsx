import { buildQueryString } from "@marketplace/utils";
import { marketplaceApi } from "@/lib/api";
import { ProductCard } from "@/components/product/ProductCard";
export default async function MarketplacePage({ searchParams }: { searchParams: Promise<Record<string, string | undefined>> }) { const params = await searchParams; const query = buildQueryString(params); const result = await marketplaceApi.products(query ? `?${query}` : "").catch(() => ({ data: [], page: 1, totalPages: 1, total: 0 })); return <main className="container-page py-10"><h1 className="text-4xl font-black">Marketplace</h1><form className="card mt-6 grid gap-4 p-4 md:grid-cols-4"><input className="input" name="q" placeholder="Search products" defaultValue={params.q}/><input className="input" name="category" placeholder="Category" defaultValue={params.category}/><select className="input" name="sort" defaultValue={params.sort ?? "newest"}><option value="newest">Newest</option><option value="price_asc">Price: low to high</option><option value="price_desc">Price: high to low</option><option value="rating">Top rated</option></select><button className="btn-primary">Apply filters</button></form><p className="mt-6 text-sm text-slate-600">{result.total} products found</p><div className="mt-4 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{result.data.map((product) => <ProductCard key={product.id} product={product}/>)}</div></main>; }
